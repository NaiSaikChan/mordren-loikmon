# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# TypeScript compiler emit artifacts — vue-tsc must always run with --noEmit
# These .js/.js.map files inside src/ are never intentional; delete & ignore them
packages/web/src/**/*.js
packages/web/src/**/*.js.map

# Mobile — npm creates this when running `npx expo install` inside a pnpm workspace
packages/mobile/package-lock.json

# Env files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Expo / React Native (future)
.expo/
android/
ios/

# Agents Skills
.agents
.github